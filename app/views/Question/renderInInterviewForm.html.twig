<a href="{{ question.updateHref }}" class="fr">edit</a>
<label>
	<input type="checkbox" name="questions[]" value="{{ question.id }}"{% if question.checked %} checked="checked"{% endif %} />
	{{ question.shortText }}
</label>